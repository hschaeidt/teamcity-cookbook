# -*- mode: ruby -*-
# encoding: utf-8

site :opscode

metadata

group :test do
  cookbook "apt"
  cookbook "teamcity_server_test", :path => "./test/cookbooks/teamcity_server_test"
  cookbook "minitest-handler", "0.1.7"
end
